select extract(dow from time at time zone 'America/Phoenix') as t, count(*) as y from marks where user_id = $1 and mood = $2 and time at time zone 'America/Phoenix' >= date_trunc('week', now() at time zone 'America/Phoenix') group by t order by t;
